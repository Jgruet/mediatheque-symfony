<form method="post" action="{{ path('cd_delete', {'id': cd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cd.id) }}">
    <button class="btn">Delete</button>
</form>
